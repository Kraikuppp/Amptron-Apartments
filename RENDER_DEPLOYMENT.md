# 🚀 Billing Rental System - Render Deployment Guide (No Database)

## ภาพรวม
โปรเจคนี้ใช้ **mock data** ไม่ต้องการฐานข้อมูล สามารถ deploy บน Render.com ได้ง่ายมากครับ

---

## 📋 ขั้นตอนการ Deploy

### ขั้นตอนที่ 1: เตรียม GitHub Repository

1. **สร้าง GitHub repository ใหม่** (ถ้ายังไม่มี)
   - ไปที่ https://github.com/new
   - ตั้งชื่อ repository เช่น `billing-rental-system`
   - เลือก Public หรือ Private ตามต้องการ
   - คลิก **Create repository**

2. **Push โค้ดขึ้น GitHub**
   ```bash
   # เปิด Terminal/PowerShell ในโฟลเดอร์โปรเจค
   cd c:\xampp\htdocs\billing
   
   # Initialize Git (ถ้ายังไม่ได้ทำ)
   git init
   
   # Add ไฟล์ทั้งหมด
   git add .
   
   # Commit
   git commit -m "Initial commit - Ready for Render deployment"
   
   # เชื่อมต่อกับ GitHub (แทน YOUR_USERNAME และ YOUR_REPO)
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
   
   # Push ขึ้น GitHub
   git branch -M main
   git push -u origin main
   ```

---

### ขั้นตอนที่ 2: Deploy บน Render.com

1. **ไปที่ Render Dashboard**
   - เข้า https://dashboard.render.com/
   - Login หรือ Sign up (ใช้ GitHub account ได้)

2. **สร้าง Web Service**
   - คลิกปุ่ม **New +** (มุมขวาบน)
   - เลือก **Web Service**

3. **เชื่อมต่อ GitHub Repository**
   - คลิก **Connect** ที่ repository ของคุณ
   - ถ้ายังไม่เห็น repository ให้คลิก **Configure account** เพื่อให้สิทธิ์ Render เข้าถึง

4. **ตั้งค่า Web Service**
   กรอกข้อมูลดังนี้:
   
   | ฟิลด์ | ค่าที่ตั้ง |
   |------|-----------|
   | **Name** | `billing-rental-system` (หรือชื่อที่ต้องการ) |
   | **Region** | `Singapore` (ใกล้ไทยที่สุด) |
   | **Branch** | `main` |
   | **Root Directory** | ว่างไว้ |
   | **Environment** | `PHP` |
   | **Build Command** | `chmod +x build.sh && ./build.sh` |
   | **Start Command** | `php -S 0.0.0.0:$PORT -t .` |
   | **Plan** | `Free` |

5. **เพิ่ม Environment Variables (ถ้าต้องการ)**
   - คลิกที่ **Advanced**
   - เพิ่มตัวแปร:
     - `PHP_TIMEZONE` = `Asia/Bangkok`
     - `APP_ENV` = `production`

6. **Deploy!**
   - คลิกปุ่ม **Create Web Service**
   - รอสักครู่ Render จะทำการ build และ deploy
   - ระหว่างนี้คุณจะเห็น logs แสดงความคืบหน้า

---

### ขั้นตอนที่ 3: เข้าใช้งาน

1. **รอจนกว่า Deploy เสร็จ**
   - จะเห็นสถานะเป็น **Live** (สีเขียว)
   - ใช้เวลาประมาณ 2-5 นาที

2. **เข้าใช้งานเว็บไซต์**
   - คุณจะได้ URL เช่น: `https://billing-rental-system.onrender.com`
   - คลิกที่ URL หรือคัดลอกไปเปิดในเบราว์เซอร์
   - **⚠️ สำคัญ: ใช้ URL นี้ ไม่ใช่ localhost!**

---

## 🎯 วิธีแก้ปัญหา ERR_CONNECTION_REFUSED

### ปัญหา: ไม่สามารถเข้า localhost ได้
**สาเหตุ:** หลังจาก deploy บน Render แล้ว เว็บไซต์ไม่ได้อยู่บนเครื่องคุณแล้ว

**วิธีแก้:**
- ✅ **ใช้ URL จาก Render**: `https://your-app.onrender.com`
- ❌ **อย่าใช้**: `http://localhost` หรือ `http://127.0.0.1`

### ตัวอย่าง URL ที่ถูกต้อง:
```
https://billing-rental-system.onrender.com
https://billing-rental-system.onrender.com/index.php
https://billing-rental-system.onrender.com/room.php
```

---

## 📌 ข้อควรรู้เกี่ยวกับ Render Free Plan

### ✅ ข้อดี:
- ฟรี 100%
- รองรับ PHP
- ได้ HTTPS ฟรี
- Auto deploy เมื่อ push GitHub
- Bandwidth 100GB/เดือน

### ⚠️ ข้อจำกัด:
- **Cold Start**: Service จะ sleep หลังไม่มีคนเข้าใช้ 15 นาที
- **Wake Up Time**: ครั้งแรกที่เข้าหลัง sleep จะใช้เวลา 30-60 วินาที
- **Performance**: ช้ากว่า Paid Plan

### 💡 เคล็ดลับ:
- ถ้าต้องการให้เว็บไซต์ตื่นตลอดเวลา ใช้ **UptimeRobot** (ฟรี) ping ทุก 5 นาที
- หรืออัพเกรดเป็น Paid Plan ($7/เดือน) จะไม่มี cold start

---

## 🔄 การอัพเดทโค้ด

เมื่อต้องการแก้ไขโค้ด:

```bash
# แก้ไขไฟล์ตามต้องการ
# จากนั้น commit และ push

git add .
git commit -m "Update: คำอธิบายการแก้ไข"
git push

# Render จะ auto-deploy ให้อัตโนมัติ!
```

---

## 🆘 การแก้ปัญหา

### 1. Build Failed
- ตรวจสอบ logs ใน Render Dashboard
- ดูว่า build.sh มีปัญหาหรือไม่
- ตรวจสอบว่า PHP syntax ถูกต้อง

### 2. Service ไม่ทำงาน
- ตรวจสอบ logs แท็บ "Logs"
- ดูว่า Start Command ถูกต้องหรือไม่
- ตรวจสอบว่า PORT variable ถูกใช้ถูกต้อง

### 3. หน้าเว็บแสดงผลไม่ถูกต้อง
- ตรวจสอบ path ของ CSS/JS ให้ใช้ relative path
- ตรวจสอบว่า images/assets อยู่ใน repository

### 4. ต้องการดู Logs
- ไปที่ Render Dashboard → เลือก Service → แท็บ "Logs"
- จะเห็น real-time logs ของ application

---

## 📞 ติดต่อ Support

- **Render Docs**: https://render.com/docs
- **Render Community**: https://community.render.com/
- **Status Page**: https://status.render.com/

---

## 🎉 เสร็จแล้ว!

ตอนนี้เว็บไซต์ของคุณ online แล้ว สามารถแชร์ URL ให้คนอื่นเข้าใช้งานได้เลย!

**URL ของคุณ:** `https://[your-service-name].onrender.com`

---

## 📝 Checklist

- [ ] Push โค้ดขึ้น GitHub
- [ ] สร้าง Web Service บน Render
- [ ] ตั้งค่า Build และ Start Command
- [ ] รอ Deploy เสร็จ
- [ ] ทดสอบเข้าใช้งานผ่าน URL ที่ได้
- [ ] ✅ เสร็จสมบูรณ์!
