# 🚀 คู่มือการ Deploy โปรเจค Billing Rental System บน Render.com

## 📌 ข้อมูลสำคัญ
- **ไม่ใช้ฐานข้อมูล**: โปรเจคนี้ใช้ Mock Data ทั้งหมด
- **ใช้ Docker**: เนื่องจาก Render ไม่รองรับ PHP environment โดยตรง
- **ฟรี 100%**: ใช้ Render Free Plan

---

## 📋 สิ่งที่ต้องเตรียม

1. ✅ บัญชี GitHub (สมัครฟรีที่ https://github.com)
2. ✅ บัญชี Render (สมัครฟรีที่ https://render.com)
3. ✅ Git ติดตั้งบนเครื่อง (ดาวน์โหลดที่ https://git-scm.com)

---

## 🎯 ขั้นตอนที่ 1: เตรียม GitHub Repository

### 1.1 สร้าง Repository ใหม่บน GitHub

1. เข้า https://github.com/new
2. กรอกข้อมูล:
   - **Repository name**: `billing-rental-system` (หรือชื่อที่ต้องการ)
   - **Description**: `Apartment Billing and Rental Management System`
   - **Visibility**: เลือก **Public** (สำคัญ! Render Free Plan ต้องใช้ Public repo)
3. **อย่า**เลือก "Add a README file" หรือ ".gitignore" หรือ "license"
4. คลิก **Create repository**

### 1.2 Push โค้ดขึ้น GitHub

เปิด **PowerShell** หรือ **Command Prompt** แล้วรันคำสั่งต่อไปนี้:

```powershell
# 1. ไปที่โฟลเดอร์โปรเจค
cd c:\xampp\htdocs\billing

# 2. ตรวจสอบว่ามี Git repository อยู่แล้วหรือไม่
git status

# 3. ถ้ายังไม่มี ให้ init ใหม่
git init

# 4. เพิ่มไฟล์ทั้งหมด
git add .

# 5. Commit
git commit -m "Initial commit - Ready for Render deployment with Docker"

# 6. เชื่อมต่อกับ GitHub (แทน YOUR_USERNAME ด้วยชื่อ GitHub ของคุณ)
git remote add origin https://github.com/YOUR_USERNAME/billing-rental-system.git

# หรือถ้ามี remote อยู่แล้ว ให้เปลี่ยน URL
git remote set-url origin https://github.com/YOUR_USERNAME/billing-rental-system.git

# 7. Push ขึ้น GitHub
git branch -M main
git push -u origin main
```

**หมายเหตุ**: แทน `YOUR_USERNAME` ด้วยชื่อผู้ใช้ GitHub ของคุณ

### 1.3 ตรวจสอบว่า Push สำเร็จ

1. เข้า https://github.com/YOUR_USERNAME/billing-rental-system
2. ตรวจสอบว่าไฟล์ทั้งหมดอัพโหลดแล้ว โดยเฉพาะ:
   - ✅ `Dockerfile`
   - ✅ `render.yaml`
   - ✅ `index.php`
   - ✅ โฟลเดอร์ `includes/`, `business/`, `admin/`

---

## 🚀 ขั้นตอนที่ 2: สร้าง Web Service บน Render

### 2.1 เข้าสู่ Render Dashboard

1. เข้า https://dashboard.render.com
2. **Sign Up** หรือ **Log In**
   - แนะนำให้ใช้ **Sign in with GitHub** เพื่อความสะดวก
3. ถ้าเป็นครั้งแรก ให้อนุญาต Render เข้าถึง GitHub

### 2.2 สร้าง Web Service

1. คลิกปุ่ม **New +** (มุมขวาบน)
2. เลือก **Web Service**

### 2.3 เชื่อมต่อ Repository

1. คุณจะเห็นรายการ repositories จาก GitHub
2. ค้นหา `billing-rental-system`
3. คลิกปุ่ม **Connect** ข้างชื่อ repository

   **ถ้าไม่เห็น repository:**
   - คลิก **Configure account** (ด้านล่าง)
   - เลือก repositories ที่ต้องการให้ Render เข้าถึง
   - กลับมาหน้านี้อีกครั้ง

### 2.4 ตั้งค่า Web Service

กรอกข้อมูลตามนี้:

| ฟิลด์ | ค่าที่ตั้ง | คำอธิบาย |
|------|-----------|----------|
| **Name** | `billing-rental-system` | ชื่อ service (จะเป็นส่วนหนึ่งของ URL) |
| **Region** | `Singapore` | ใกล้ไทยที่สุด ความเร็วดีที่สุด |
| **Branch** | `main` | branch ที่ต้องการ deploy |
| **Root Directory** | (ว่างไว้) | ใช้ root ของ repository |
| **Runtime** | `Docker` | จะตรวจจับอัตโนมัติจาก Dockerfile |

**สำคัญ**: Render จะอ่านค่าจาก `render.yaml` อัตโนมัติ ดังนั้นคุณไม่ต้องกรอก Build Command และ Start Command

### 2.5 ตั้งค่า Environment Variables (ถ้าต้องการ)

คลิก **Advanced** แล้วเพิ่มตัวแปรเหล่านี้ (ถ้ายังไม่มีใน render.yaml):

| Key | Value |
|-----|-------|
| `PORT` | `80` |
| `PHP_TIMEZONE` | `Asia/Bangkok` |
| `APP_ENV` | `production` |

**หมายเหตุ**: ถ้าคุณใช้ `render.yaml` ที่ฉันสร้างให้ ตัวแปรเหล่านี้จะถูกตั้งค่าอัตโนมัติแล้ว

### 2.6 เลือก Plan

- เลือก **Free** (ฟรี 100%)
- คลิกปุ่ม **Create Web Service**

---

## ⏳ ขั้นตอนที่ 3: รอการ Deploy

### 3.1 ติดตามความคืบหน้า

1. Render จะเริ่ม build Docker image
2. คุณจะเห็น **Logs** แสดงความคืบหน้า:
   ```
   ==> Building...
   ==> Downloading base image
   ==> Installing dependencies
   ==> Starting service
   ==> Your service is live 🎉
   ```
3. ใช้เวลาประมาณ **3-7 นาที** ในการ deploy ครั้งแรก

### 3.2 ตรวจสอบสถานะ

- รอจนกว่าสถานะเป็น **Live** (สีเขียว)
- ถ้ามีปัญหา จะแสดงสถานะ **Build failed** (สีแดง)

---

## 🎉 ขั้นตอนที่ 4: เข้าใช้งานเว็บไซต์

### 4.1 รับ URL

เมื่อ deploy สำเร็จ คุณจะได้ URL ในรูปแบบ:
```
https://billing-rental-system.onrender.com
```

### 4.2 ทดสอบเว็บไซต์

1. คลิกที่ URL หรือคัดลอกไปเปิดในเบราว์เซอร์
2. ทดสอบหน้าต่างๆ:
   - ✅ หน้าแรก: `https://your-app.onrender.com/`
   - ✅ ค้นหาห้อง: `https://your-app.onrender.com/room-search.php`
   - ✅ ข่าวสาร: `https://your-app.onrender.com/news.php`
   - ✅ Login: คลิกที่โลโก้ แล้ว login ด้วย:
     - Username: `amptr`
     - Password: `amptr`

### 4.3 ทดสอบ Business Dashboard

1. Login ด้วย Business Account:
   - Username: `business`
   - Password: `business123`
2. เข้า Dashboard: `https://your-app.onrender.com/business/dashboard.php`

---

## 🔄 การอัพเดทโค้ด

เมื่อต้องการแก้ไขโค้ด:

```powershell
# 1. แก้ไขไฟล์ตามต้องการ

# 2. Commit และ Push
git add .
git commit -m "Update: คำอธิบายการแก้ไข"
git push

# 3. Render จะ Auto-Deploy ให้อัตโนมัติ!
```

**หมายเหตุ**: 
- Render จะตรวจจับการ push ใหม่และ deploy อัตโนมัติ
- ใช้เวลาประมาณ 3-5 นาทีในการ deploy ใหม่

---

## ⚠️ ข้อควรรู้เกี่ยวกับ Render Free Plan

### ✅ ข้อดี

- ✅ **ฟรี 100%** ไม่ต้องใส่บัตรเครดิต
- ✅ **HTTPS ฟรี** พร้อม SSL certificate
- ✅ **Auto Deploy** เมื่อ push GitHub
- ✅ **Bandwidth 100GB/เดือน**
- ✅ **750 ชั่วโมง/เดือน** (เพียงพอสำหรับ 1 service)

### ⚠️ ข้อจำกัด

- ⚠️ **Cold Start**: Service จะ sleep หลังไม่มีคนเข้าใช้ **15 นาที**
- ⚠️ **Wake Up Time**: ครั้งแรกที่เข้าหลัง sleep จะใช้เวลา **30-60 วินาที**
- ⚠️ **Performance**: ช้ากว่า Paid Plan
- ⚠️ **Public Repository เท่านั้น**: Free Plan ต้องใช้ Public GitHub repo

### 💡 เคล็ดลับ: ป้องกัน Cold Start

**วิธีที่ 1: ใช้ UptimeRobot (แนะนำ)**

1. สมัครฟรีที่ https://uptimerobot.com
2. สร้าง Monitor ใหม่:
   - Monitor Type: `HTTP(s)`
   - URL: `https://your-app.onrender.com`
   - Monitoring Interval: `5 minutes`
3. UptimeRobot จะ ping ทุก 5 นาที ทำให้ service ไม่ sleep

**วิธีที่ 2: อัพเกรดเป็น Paid Plan**
- ราคา $7/เดือน
- ไม่มี cold start
- Performance ดีกว่า

---

## 🆘 การแก้ปัญหา

### ❌ ปัญหา: Build Failed

**สาเหตุที่เป็นไปได้:**
1. Dockerfile มีปัญหา
2. ไฟล์ที่จำเป็นไม่ได้ push ขึ้น GitHub
3. PHP syntax error

**วิธีแก้:**
1. ดู Logs ใน Render Dashboard → แท็บ "Logs"
2. ตรวจสอบข้อความ error
3. แก้ไขแล้ว push ใหม่

### ❌ ปัญหา: Service ไม่ทำงาน (Live แต่เข้าไม่ได้)

**วิธีแก้:**
1. ตรวจสอบ Logs แท็บ "Logs"
2. ดูว่า Apache start สำเร็จหรือไม่
3. ตรวจสอบ PORT variable

### ❌ ปัญหา: หน้าเว็บแสดงผลไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบ path ของ CSS/JS ให้ใช้ relative path
2. ตรวจสอบว่า images/assets อยู่ใน repository
3. เปิด Browser Console (F12) ดู error

### ❌ ปัญหา: ERR_CONNECTION_REFUSED

**สาเหตุ:** คุณกำลังพยายามเข้า localhost

**วิธีแก้:**
- ✅ **ใช้**: `https://your-app.onrender.com`
- ❌ **อย่าใช้**: `http://localhost` หรือ `http://127.0.0.1`

### ❌ ปัญหา: ช้ามาก (Cold Start)

**สาเหตุ:** Service เพิ่ง wake up จาก sleep

**วิธีแก้:**
1. รอ 30-60 วินาที
2. ใช้ UptimeRobot (ดูด้านบน)
3. หรืออัพเกรดเป็น Paid Plan

---

## 📊 การดู Logs และ Metrics

### ดู Real-time Logs

1. ไปที่ Render Dashboard
2. เลือก Service ของคุณ
3. คลิกแท็บ **Logs**
4. จะเห็น logs แบบ real-time

### ดู Metrics

1. คลิกแท็บ **Metrics**
2. จะเห็น:
   - CPU Usage
   - Memory Usage
   - Request Count
   - Response Time

---

## 🔒 Security Best Practices

### 1. ใช้ Environment Variables สำหรับข้อมูลสำคัญ

ถ้าในอนาคตต้องการเพิ่มฐานข้อมูล:

```yaml
envVars:
  - key: DB_HOST
    value: your-database-host
  - key: DB_PASSWORD
    value: your-secure-password
```

### 2. เปิด HTTPS เท่านั้น

Render ให้ HTTPS ฟรี และบังคับใช้อัตโนมัติ

### 3. ตั้งค่า Security Headers

ตรวจสอบว่า `.htaccess` มี security headers (มีอยู่แล้วในโปรเจค)

---

## 📞 ช่องทางติดต่อและเอกสาร

- **Render Documentation**: https://render.com/docs
- **Render Community Forum**: https://community.render.com
- **Render Status**: https://status.render.com
- **Docker Documentation**: https://docs.docker.com

---

## 📝 Checklist การ Deploy

ใช้ checklist นี้เพื่อตรวจสอบว่าทำครบทุกขั้นตอน:

### เตรียมการ
- [ ] มีบัญชี GitHub แล้ว
- [ ] มีบัญชี Render แล้ว
- [ ] ติดตั้ง Git บนเครื่องแล้ว

### GitHub
- [ ] สร้าง Public Repository แล้ว
- [ ] Push โค้ดขึ้น GitHub แล้ว
- [ ] ตรวจสอบว่ามี `Dockerfile` และ `render.yaml`

### Render
- [ ] สร้าง Web Service แล้ว
- [ ] เชื่อมต่อ GitHub Repository แล้ว
- [ ] ตั้งค่า Region เป็น Singapore
- [ ] เลือก Free Plan
- [ ] คลิก Create Web Service

### ทดสอบ
- [ ] รอ Deploy เสร็จ (สถานะ Live)
- [ ] เข้าใช้งานผ่าน URL ที่ได้
- [ ] ทดสอบ Login (amptr/amptr)
- [ ] ทดสอบ Business Dashboard (business/business123)
- [ ] ทดสอบหน้าต่างๆ ทำงานปกติ

### Optional
- [ ] ตั้งค่า UptimeRobot เพื่อป้องกัน cold start
- [ ] เพิ่ม Custom Domain (ถ้าต้องการ)

---

## 🎉 เสร็จแล้ว!

ตอนนี้เว็บไซต์ของคุณ **online แล้ว** สามารถแชร์ URL ให้คนอื่นเข้าใช้งานได้เลย!

**URL ของคุณ:** `https://[your-service-name].onrender.com`

### ขั้นตอนต่อไป (Optional)

1. **Custom Domain**: เพิ่มโดเมนของคุณเอง
2. **UptimeRobot**: ป้องกัน cold start
3. **Analytics**: ติดตั้ง Google Analytics
4. **Monitoring**: ตั้งค่า error monitoring

---

## 💬 ต้องการความช่วยเหลือ?

ถ้ามีปัญหาหรือคำถาม:
1. ตรวจสอบ Logs ใน Render Dashboard
2. อ่าน Troubleshooting ด้านบน
3. ถามใน Render Community Forum
4. ดู Render Documentation

---

**สร้างโดย**: Antigravity AI Assistant  
**อัพเดทล่าสุด**: 2025-11-20  
**เวอร์ชัน**: 2.0 (Docker-based deployment)
