# 🔐 คู่มือการเข้าสู่ระบบ (Login Guide)

## 📋 สรุป

ระบบได้รับการปรับปรุงให้สามารถ **login โดยไม่ต้องใช้ SQL** ผ่านบัญชีทดสอบที่สร้างไว้ในระบบ

---

## 👥 บัญชีทดสอบ (Test Accounts)

### 1. 👑 Admin (ผู้ดูแลระบบ)
```
Username: admin
Password: admin
```

**สิทธิ์การใช้งาน:**
- ✅ จัดการผู้ใช้ทั้งหมด
- ✅ จัดการผู้ประกอบการ
- ✅ ดูรายงานทั้งระบบ
- ✅ จัดการหมวดหมู่และการตั้งค่า
- ✅ อนุมัติ/ปฏิเสธการลงทะเบียน
- ✅ ดู Activity Logs

**หลังจาก Login:**
- Redirect ไปที่: `admin/index.php`
- Dashboard แสดงภาพรวมทั้งระบบ

---

### 2. 🏢 Business (ผู้ประกอบการ)
```
Username: business
Password: business
```

**สิทธิ์การใช้งาน:**
- ✅ จัดการห้องพัก/ที่พักของตนเอง
- ✅ จัดการสินค้า/อุปกรณ์ไฟฟ้า
- ✅ ดูรายงานการขายของตนเอง
- ✅ อัพเดทข้อมูลธุรกิจ
- ✅ จัดการรูปภาพและโปรโมชั่น
- ❌ ไม่สามารถดูข้อมูลธุรกิจอื่นได้

**ข้อมูลธุรกิจ:**
- Business ID: 1
- Business Name: ธุรกิจตัวอย่าง
- Business Type: both (ทั้งห้องพักและสินค้า)

**หลังจาก Login:**
- Redirect ไปที่: `business/dashboard.php`
- Dashboard แสดงข้อมูลธุรกิจ

---

### 3. 👤 User (ผู้ใช้ทั่วไป)
```
Username: user
Password: user
```

**สิทธิ์การใช้งาน:**
- ✅ ค้นหาห้องพัก/ที่พัก
- ✅ ค้นหาสินค้า
- ✅ บันทึกรายการโปรด
- ✅ ดูประวัติการค้นหา
- ❌ ไม่สามารถเพิ่ม/แก้ไขข้อมูลได้

**หลังจาก Login:**
- Redirect ไปที่: `index.php`
- กลับไปหน้าแรกพร้อมสิทธิ์ผู้ใช้

---

## 🚀 วิธีการ Login

### วิธีที่ 1: Login แบบปกติ
1. ไปที่หน้า `login.php`
2. กรอก Username และ Password
3. กดปุ่ม "เข้าสู่ระบบ"

### วิธีที่ 2: Quick Login (ปุ่มด่วน)
1. ไปที่หน้า `login.php`
2. กดปุ่มด่วนที่ต้องการ:
   - 🛡️ **Login as Admin** - เข้าระบบแบบ Admin
   - 🏪 **Login as Business** - เข้าระบบแบบผู้ประกอบการ
   - 👤 **Login as User** - เข้าระบบแบบผู้ใช้ทั่วไป

---

## 🔧 การทำงานของระบบ

### Test Mode (ไม่ต้องใช้ SQL)
ระบบจะตรวจสอบ username/password จากข้อมูลที่ hard-coded ในไฟล์ `includes/auth.php`

**ข้อดี:**
- ✅ ไม่ต้องติดตั้งฐานข้อมูล
- ✅ ทดสอบได้ทันที
- ✅ เหมาะสำหรับ Development

**ข้อจำกัด:**
- ⚠️ ข้อมูล session จะหายเมื่อปิดเบราว์เซอร์
- ⚠️ ไม่สามารถสร้างผู้ใช้ใหม่ได้ (ใช้เฉพาะ 3 บัญชีทดสอบ)
- ⚠️ Activity logs จะไม่ถูกบันทึก

### Database Mode (ใช้ SQL)
หากมีการเชื่อมต่อฐานข้อมูล ระบบจะ:
1. ตรวจสอบ test accounts ก่อน
2. ถ้าไม่ตรง จะค้นหาในฐานข้อมูล
3. ตรวจสอบ password ด้วย `password_verify()`

---

## 📊 ข้อมูล Session

เมื่อ Login สำเร็จ ระบบจะเก็บข้อมูลใน Session:

### สำหรับ Admin & User
```php
$_SESSION['user_id']      // ID ของผู้ใช้
$_SESSION['username']     // Username
$_SESSION['email']        // Email
$_SESSION['full_name']    // ชื่อ-นามสกุล
$_SESSION['role']         // Role: admin/business/user
```

### สำหรับ Business (เพิ่มเติม)
```php
$_SESSION['business_id']   // ID ธุรกิจ
$_SESSION['business_name'] // ชื่อธุรกิจ
$_SESSION['business_type'] // ประเภท: rooms/products/both
```

---

## 🔒 การตรวจสอบสิทธิ์

### ในไฟล์ PHP
```php
// ตรวจสอบว่า login แล้วหรือยัง
if (!isLoggedIn()) {
    redirect('login.php');
}

// ตรวจสอบว่าเป็น Admin หรือไม่
if (!isAdmin()) {
    die('Access Denied');
}

// ตรวจสอบว่าเป็น Business หรือไม่
if (!isBusiness()) {
    die('Access Denied');
}
```

### ตรวจสอบหลายบทบาท
```php
// อนุญาตให้ Admin และ Business เข้าถึง
if (!isAdmin() && !isBusiness()) {
    die('Access Denied');
}
```

---

## 🚪 การออกจากระบบ (Logout)

### วิธี Logout
1. คลิกที่ปุ่ม "ออกจากระบบ" ใน header
2. หรือไปที่ `logout.php` โดยตรง

### สิ่งที่เกิดขึ้นเมื่อ Logout
- ✅ Session ทั้งหมดถูกลบ
- ✅ Cookie session ถูกลบ
- ✅ บันทึก activity log (ถ้ามี DB)
- ✅ Redirect กลับไป `index.php`

---

## 🎯 Use Cases

### Use Case 1: ทดสอบระบบ Admin
```
1. Login ด้วย admin/admin
2. ไปที่ admin/index.php
3. ทดสอบฟีเจอร์ต่างๆ
4. Logout
```

### Use Case 2: ทดสอบระบบผู้ประกอบการ
```
1. Login ด้วย business/business
2. ไปที่ business/dashboard.php
3. เพิ่ม/แก้ไข ห้องพักหรือสินค้า
4. Logout
```

### Use Case 3: ทดสอบระบบค้นหา
```
1. Login ด้วย user/user
2. ใช้งาน search bar ที่หน้าแรก
3. ดูรายละเอียดห้องพัก
4. Logout
```

---

## ⚙️ ไฟล์ที่เกี่ยวข้อง

### Core Files
- `login.php` - หน้า Login Form
- `logout.php` - ประมวลผล Logout
- `includes/auth.php` - ฟังก์ชัน Authentication
- `includes/functions.php` - Helper functions

### Protected Pages
- `admin/` - ต้องเป็น Admin
- `business/` - ต้องเป็น Business
- `profile.php` - ต้อง Login

---

## 🐛 Troubleshooting

### ปัญหา: Login แล้วไม่ redirect
**แก้ไข:**
- ตรวจสอบว่าเปิด session แล้วหรือยัง
- ดูว่ามี error ใน PHP logs
- ตรวจสอบ permissions ของ session folder

### ปัญหา: Session หายเร็ว
**แก้ไข:**
- ตั้งค่า session timeout ใน `php.ini`
- ใช้ "Remember me" feature (ถ้ามี)

### ปัญหา: ไม่สามารถเข้า Admin/Business
**แก้ไข:**
- ตรวจสอบว่า login ด้วย username/password ที่ถูกต้อง
- ตรวจสอบว่าไฟล์ `includes/auth_check.php` ทำงานถูกต้อง

---

## 🔐 Security Notes

### ⚠️ สำคัญ: สำหรับ Production
1. **เปลี่ยน Password** - อย่าใช้ admin/admin ใน Production
2. **ลบ Test Accounts** - ลบ hard-coded test users
3. **ใช้ Database** - เก็บข้อมูลใน database จริง
4. **HTTPS** - ใช้ HTTPS สำหรับ login
5. **Rate Limiting** - จำกัดจำนวนครั้งที่ login ผิด

### ✅ Best Practices
- ใช้ `password_hash()` สำหรับเก็บ password
- ใช้ `password_verify()` สำหรับตรวจสอบ
- ตั้งค่า session timeout ที่เหมาะสม
- บันทึก login attempts ใน logs

---

## 📝 สรุป

| บัญชี | Username | Password | Role | Dashboard |
|-------|----------|----------|------|-----------|
| Admin | `admin` | `admin` | admin | admin/index.php |
| Business | `business` | `business` | business | business/dashboard.php |
| User | `user` | `user` | user | index.php |

---

## 🚀 Quick Start

### เริ่มต้นใช้งาน 3 ขั้นตอน:
1. เปิดเบราว์เซอร์ไปที่ `http://localhost/billing/login.php`
2. กดปุ่ม "Login as Admin" หรือ "Login as Business"
3. เริ่มใช้งานได้เลย!

---

## 📞 Support

หากมีปัญหาหรือข้อสงสัย:
- ตรวจสอบไฟล์ `includes/auth.php` บรรทัด 93-151
- อ่าน error message ที่แสดง
- ตรวจสอบ PHP error logs

---

**อัพเดทล่าสุด:** 2024
**เวอร์ชั่น:** 2.0 - No SQL Required