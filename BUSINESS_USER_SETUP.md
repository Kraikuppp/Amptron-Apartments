# Business User System - คู่มือการใช้งาน

## 📋 สารบัญ
1. [ภาพรวมระบบ](#ภาพรวมระบบ)
2. [การติดตั้ง](#การติดตั้ง)
3. [ข้อมูลการเข้าสู่ระบบ](#ข้อมูลการเข้าสู่ระบบ)
4. [ฟีเจอร์สำหรับผู้ประกอบการ](#ฟีเจอร์สำหรับผู้ประกอบการ)
5. [โครงสร้างฐานข้อมูล](#โครงสร้างฐานข้อมูล)
6. [API & Functions](#api--functions)
7. [การแก้ไขปัญหา](#การแก้ไขปัญหา)

---

## 🎯 ภาพรวมระบบ

ระบบผู้ประกอบการ (Business User System) เป็นส่วนหนึ่งของ RentHub Platform ที่ออกแบบมาเพื่อให้ผู้ประกอบการสามารถจัดการห้องเช่าของตนเองได้อย่างมีประสิทธิภาพ

### ความสามารถหลัก
- ✅ จัดการห้องเช่าของตัวเอง (CRUD)
- ✅ อัปโหลดรูปภาพห้อง
- ✅ ปักหมุดตำแหน่งบนแผนที่
- ✅ สร้างโปรโมชัน/ส่วนลด
- ✅ ดูสถิติและรายงาน
- ✅ ระบบข้อความ/แชท
- ✅ จัดการโปรไฟล์ธุรกิจ
- ✅ ระบบยืนยันตัวตน
- ✅ แพ็กเกจสมาชิก

---

## 🚀 การติดตั้ง

### ขั้นตอนที่ 1: รัน SQL Setup Script

```bash
# เข้าสู่ MySQL/MariaDB
mysql -u root -p billing

# รัน SQL Script
source setup_business_user.sql
```

หรือผ่าน phpMyAdmin:
1. เปิด phpMyAdmin
2. เลือกฐานข้อมูล `billing`
3. ไปที่แท็บ "SQL"
4. Copy เนื้อหาจากไฟล์ `setup_business_user.sql`
5. Paste และคลิก "Go"

### ขั้นตอนที่ 2: ตรวจสอบการติดตั้ง

```sql
-- ตรวจสอบ Business User
SELECT * FROM users WHERE username = 'business';

-- ตรวจสอบ Business Profile
SELECT * FROM business_profiles WHERE user_id = (SELECT id FROM users WHERE username = 'business');

-- ตรวจสอบห้องเช่า
SELECT id, title, status, price FROM business_properties LIMIT 10;
```

### ขั้นตอนที่ 3: ทดสอบเข้าสู่ระบบ

1. เปิดเบราว์เซอร์ไปที่: `http://localhost/billing/login.php`
2. ใช้ข้อมูลการเข้าสู่ระบบด้านล่าง

---

## 🔐 ข้อมูลการเข้าสู่ระบบ

### Business User (ผู้ประกอบการ)
```
Username: business
Password: business123
Email: business@renthub.com
Role: business
```

### Admin User (ผู้ดูแลระบบ)
```
Username: admin
Password: admin123
Email: admin@renthub.com
Role: admin
```

### Customer User (ผู้ใช้ทั่วไป)
```
Username: customer
Password: customer123
Email: customer@renthub.com
Role: customer
```

---

## ✨ ฟีเจอร์สำหรับผู้ประกอบการ

### 1. Dashboard หลัก (`/business/dashboard.php`)
- ภาพรวมธุรกิจ
- สถิติยอดเข้าชม, ห้องว่าง, ยอดติดต่อ
- กราฟแสดงสถิติ 7 วันล่าสุด
- ห้องล่าสุด
- โปรโมชันที่กำลังใช้งาน

### 2. จัดการห้องเช่า (`/business/properties.php`)
- แสดงห้องทั้งหมด
- Filter: สถานะ, เขต, ราคา
- เรียงลำดับ: ราคา, วันที่, ยอดเข้าชม
- Quick actions: แก้ไข, ลบ, เปลี่ยนสถานะ

### 3. เพิ่มห้องใหม่ (`/business/add-property.php`)
**ข้อมูลพื้นฐาน:**
- ชื่อห้อง
- คำอธิบาย
- ประเภทห้อง (คอนโด, อพาร์ทเมนท์, หอพัก, บ้าน)
- สถานะ (ว่าง, ให้เช่าแล้ว, ปรับปรุง, ซ่อน)

**ที่อยู่และตำแหน่ง:**
- ที่อยู่เต็ม
- เขต/จังหวัด/รหัสไปรษณีย์
- 📍 ปักหมุดบนแผนที่ (Latitude/Longitude)

**ข้อมูลห้อง:**
- ขนาดห้อง (ตร.ม.)
- จำนวนห้องนอน/ห้องน้ำ
- ชั้น

**ราคาและค่าใช้จ่าย:**
- ค่าเช่า/เดือน
- เงินประกัน
- ค่าน้ำ/ค่าไฟ/ค่าส่วนกลาง

**สิ่งอำนวยความสะดวก:**
- WiFi, แอร์, เฟอร์นิเจอร์, ฟิตเนส, สระว่ายน้ำ ฯลฯ

**อัปโหลดรูปภาพ:**
- รูปหลัก (Main Image)
- รูป Gallery (หลายรูป)
- Floor Plan

### 4. แก้ไขห้อง (`/business/edit-property.php?id=X`)
- แก้ไขข้อมูลทั้งหมด
- เปลี่ยนสถานะห้อง
- จัดการรูปภาพ
- ลบห้อง

### 5. สถิติและรายงาน (`/business/analytics.php`)
**สถิติแต่ละห้อง:**
- ยอดเข้าชมรวม
- คลิกดูรายละเอียด
- จำนวนคนบันทึก Favorite
- จำนวนคนติดต่อสอบถาม

**กราฟและแผนภูมิ:**
- กราฟเส้นแสดงยอดเข้าชมรายวัน/รายเดือน
- กราฟแท่งเปรียบเทียบห้องต่างๆ
- Top 5 ห้องที่มีคนสนใจมากที่สุด

**ส่งออกรายงาน:**
- Export เป็น PDF
- Export เป็น Excel

### 6. จัดการโปรโมชัน (`/business/promotions.php`)
**สร้างโปรโมชัน:**
- เลือกห้องที่ต้องการทำโปรโมชัน
- ประเภทส่วนลด: เปอร์เซ็นต์ หรือ จำนวนเงิน
- กำหนดวันเริ่ม-สิ้นสุด
- เปิด/ปิดการมองเห็น

**รายการโปรโมชัน:**
- โปรโมชันที่กำลังใช้งาน
- โปรโมชันที่หมดอายุ
- แก้ไข/ลบโปรโมชัน

### 7. ระบบข้อความ (`/business/messages.php`)
- Inbox: ข้อความทั้งหมด
- ข้อความที่ยังไม่อ่าน
- ตอบข้อความผู้สนใจ
- แสดง conversation แบบ chat
- แจ้งเตือนข้อความใหม่ (badge)

### 8. จัดการโปรไฟล์ธุรกิจ (`/business/profile.php`)
**ข้อมูลธุรกิจ:**
- ชื่อธุรกิจ
- ประเภทธุรกิจ
- คำอธิบาย
- ที่อยู่
- เบอร์โทร/Line ID/Facebook

**อัปโหลดโลโก้:**
- รูปโปรไฟล์/โลโก้บริษัท

**การตั้งค่า:**
- ตั้งค่าการแจ้งเตือน (Email/LINE Notify)
- เวลาที่สะดวกรับการนัดดูห้อง

### 9. ยืนยันตัวตน (`/business/verification.php`)
- อัปโหลดเอกสาร:
  - สำเนาบัตรประชาชน
  - หนังสือเช่าสถานที่
  - หนังสือจดทะเบียนหอพัก
  - ใบอนุญาตประกอบกิจการ
- ดูสถานะการยืนยัน (Pending/Verified/Rejected)
- ประวัติการยื่นเอกสาร

### 10. แพ็กเกจสมาชิก (`/business/subscription.php`)
**แพ็กเกจที่มี:**
- **Free**: 3 ห้อง, Basic features
- **Basic**: 10 ห้อง, Analytics
- **Premium**: 50 ห้อง, Priority support
- **Enterprise**: Unlimited, Custom features

**ฟีเจอร์:**
- ดูแพ็กเกจปัจจุบัน
- Upgrade แพ็กเกจ
- ประวัติการชำระเงิน
- ออกใบเสร็จ
- เติมเครดิต Boost ห้อง

---

## 🗄️ โครงสร้างฐานข้อมูล

### ตาราง `business_profiles`
เก็บข้อมูลโปรไฟล์ธุรกิจ
```sql
- id, user_id, business_name, business_type
- description, address, district, province
- verification_status (pending/verified/rejected)
- subscription_plan (free/basic/premium/enterprise)
- credits, total_properties, total_views
```

### ตาราง `business_properties`
เก็บข้อมูลห้องเช่า
```sql
- id, business_id, title, description, property_type
- status (available/rented/maintenance/hidden)
- address, district, latitude, longitude
- room_size, bedrooms, bathrooms
- price, deposit, water_cost, electricity_cost
- amenities, furniture
- views, favorites, contacts
```

### ตาราง `property_images`
เก็บรูปภาพห้อง
```sql
- id, property_id, image_path, image_type
- sort_order, caption
```

### ตาราง `property_promotions`
เก็บโปรโมชัน
```sql
- id, property_id, promotion_title
- discount_type, discount_value
- start_date, end_date, is_active
```

### ตาราง `property_analytics`
เก็บสถิติรายวัน
```sql
- id, property_id, date
- views, detail_views, favorites, contacts, shares
```

### ตาราง `business_messages`
เก็บข้อความ/แชท
```sql
- id, property_id, sender_id, receiver_id
- message, is_read, conversation_id
```

---

## 📚 API & Functions

### ฟังก์ชันสำคัญใน `includes/functions.php`

```php
// ตรวจสอบ Business User
isBusiness(): bool

// ดึงข้อมูล Business Profile
getBusinessProfile($userId): array

// ดึงห้องของ Business
getBusinessProperties($businessId, $filters = []): array

// เพิ่มห้องใหม่
addProperty($businessId, $data): int

// อัปเดตห้อง
updateProperty($propertyId, $data): bool

// ลบห้อง
deleteProperty($propertyId): bool

// อัปโหลดรูปภาพ
uploadPropertyImage($propertyId, $file, $imageType): bool

// สร้างโปรโมชัน
createPromotion($propertyId, $data): int

// บันทึกสถิติ
trackPropertyView($propertyId): void

// ส่งข้อความ
sendMessage($propertyId, $senderId, $receiverId, $message): int
```

---

## 🎨 หน้าเว็บและเส้นทาง

### Public Routes (ไม่ต้อง Login)
- `/rooms.php` - รายการห้องทั้งหมด
- `/room-detail.php?id=X` - รายละเอียดห้อง
- `/login.php` - เข้าสู่ระบบ
- `/register.php` - สมัครสมาชิก

### Business Routes (ต้อง Login เป็น Business)
- `/business/dashboard.php` - หน้าหลัก Dashboard
- `/business/properties.php` - จัดการห้องทั้งหมด
- `/business/add-property.php` - เพิ่มห้องใหม่
- `/business/edit-property.php?id=X` - แก้ไขห้อง
- `/business/analytics.php` - สถิติและรายงาน
- `/business/promotions.php` - จัดการโปรโมชัน
- `/business/messages.php` - ข้อความ/แชท
- `/business/profile.php` - โปรไฟล์ธุรกิจ
- `/business/verification.php` - ยืนยันตัวตน
- `/business/subscription.php` - แพ็กเกจสมาชิก

### Admin Routes (ต้อง Login เป็น Admin)
- `/admin/index.php` - Admin Dashboard
- `/admin/users.php` - จัดการผู้ใช้
- `/admin/properties.php` - อนุมัติห้อง
- `/admin/verifications.php` - ยืนยันตัวตนผู้ประกอบการ

---

## 🔒 การจำกัดสิทธิ์

### ผู้ประกอบการทำได้:
✅ CRUD ห้องเช่าของตัวเอง
✅ ดูสถิติห้องของตัวเอง
✅ จัดการโปรไฟล์ธุรกิจ
✅ สร้างโปรโมชัน
✅ รับ-ส่งข้อความ
✅ ดูรีวิวและตอบกลับ

### ผู้ประกอบการทำไม่ได้:
❌ ดูห้องของผู้ประกอบการรายอื่น
❌ แก้ไขข้อมูลผู้ใช้คนอื่น
❌ เข้าหน้า Admin
❌ แก้ไขการตั้งค่าระบบ
❌ ดูสถิติรวมของเว็บทั้งหมด

---

## 🛠️ การแก้ไขปัญหา

### ปัญหา: ไม่สามารถเข้าสู่ระบบได้

**วิธีแก้:**
```sql
-- ตรวจสอบ user
SELECT * FROM users WHERE username = 'business';

-- Reset password
UPDATE users 
SET password = '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi' 
WHERE username = 'business';
-- Password จะเป็น: business123
```

### ปัญหา: ไม่มี Business Profile

**วิธีแก้:**
```sql
-- สร้าง Business Profile
INSERT INTO business_profiles (user_id, business_name, verification_status)
SELECT id, full_name, 'verified' 
FROM users 
WHERE username = 'business';
```

### ปัญหา: ไม่แสดงห้องในแผนที่

**วิธีแก้:**
- ตรวจสอบว่า latitude และ longitude ถูกต้อง
- ตรวจสอบ Google Maps API Key
- ตรวจสอบ status ไม่เป็น 'hidden'

```sql
-- ตรวจสอบพิกัด
SELECT id, title, latitude, longitude, status 
FROM business_properties 
WHERE latitude IS NULL OR longitude IS NULL;
```

### ปัญหา: ไม่มีสถิติ Analytics

**วิธีแก้:**
```sql
-- สร้างข้อมูล Analytics ย้อนหลัง
INSERT INTO property_analytics (property_id, date, views, detail_views, favorites)
SELECT id, CURDATE(), 0, 0, 0
FROM business_properties;
```

### ปัญหา: Permission Denied

**วิธีแก้:**
```sql
-- ตรวจสอบ role
SELECT username, role, status FROM users WHERE username = 'business';

-- แก้ไข role
UPDATE users SET role = 'business', status = 'active' WHERE username = 'business';
```

---

## 📞 การติดต่อและสนับสนุน

- **Email**: support@renthub.com
- **Line**: @renthub
- **Facebook**: RentHub Thailand
- **Tel**: 086-341-2503

---

## 📝 Changelog

### Version 1.0.0 (2024-11-15)
- ✨ เพิ่มระบบ Business User
- ✨ Dashboard สำหรับผู้ประกอบการ
- ✨ จัดการห้องเช่า (CRUD)
- ✨ ระบบโปรโมชัน
- ✨ Analytics และ Reports
- ✨ ระบบข้อความ
- ✨ ยืนยันตัวตน
- ✨ แพ็กเกจสมาชิก

---

## 📄 License

Copyright © 2024 RentHub Thailand. All rights reserved.

---

**Happy Building! 🚀**